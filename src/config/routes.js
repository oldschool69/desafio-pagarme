const transaction = require('../api/routes/transaction')
const balance = require('../api/routes/balance')
const general = require('../api/routes/general')

module.exports = {
  transaction,
  balance,
  general,
}

